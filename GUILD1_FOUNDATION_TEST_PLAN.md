# Guild 1 E2E 테스트 계획

## 0. 적용/빌드
1. Supabase SQL Editor에서 `supabase/APPLY_GUILD1_FOUNDATION.sql` 실행.
2. `supabase/migrations/20260811_02_guild1_foundation_postcheck.sql` 실행.
3. 로컬에서 `npm ci` → `npm run build`.
4. 교사 로그인 → `길드 운영` → `Guild 1 진단`.

## 1. 기존 길드/속성 확인
- 기존 5개 길드가 교사 `길드 현황`에 표시되는지 확인.
- 기존 `guild_members.element`에서 안전하게 추론 가능한 길드는 `땅/물/빛/바람/불` 속성이 보이는지 확인.
- `속성 미설정` 길드가 있으면 `수정`에서 올바른 속성을 지정.
- 새 테스트 길드를 하나 생성했다가 `비활성`으로 바꿔 학생 배정 대상에서 빠지는지 확인.

## 2. 현재 소속 안정성
- 재학생 1명 선택 → 길드 A 배정.
- 같은 학생을 길드 B로 이동.
- 현재 화면은 B만 표시되어야 한다.
- 최근 소속 이력에는 A → B가 남아야 한다.
- DB에서 `left_at IS NULL`인 row는 그 학생에게 정확히 1개여야 한다.
- 길드 B의 **길드 속성**을 변경하면 새 membership을 만들지 않고 현재 활성 멤버의 호환용 `element`만 동기화되어야 한다.
- 종료된 과거 membership의 element는 변경되지 않아야 한다.

## 3. 전입/이동 이력
- 테스트 학생을 새로 배정한 뒤 과거 membership event가 사라지지 않는지 확인.
- 길드명을 수정해도 과거 event의 `from_guild_name/to_guild_name` snapshot이 유지되는지 확인.
- 소속 해제 후 학생 화면은 `아직 길드에 속하지 않았어요` 상태가 되는지 확인.

## 4. 시즌
- 새 시즌을 PLANNED로 생성.
- 활성화 → 기존 ACTIVE 시즌이 CLOSED가 되는지 확인.
- 종료 → CLOSED.
- 재개방 준비 → PLANNED.
- 학생 길드 헤더에서 활성 시즌명과 **월 GS / 시즌 누적 GS**가 분리되어 보이는지 확인.

## 5. 길드 세션 snapshot — 가장 중요
1. 학생 A/B/C가 길드 X, D/E가 길드 Y인 상태에서 세션 S1 생성.
2. S1의 참석 대상이 A~E로 고정됐는지 확인.
3. S1 생성 후 C를 길드 Y로 이동.
4. S1 편집 화면에서 C의 `당시 길드`는 여전히 X여야 한다.
5. 새 세션 S2 생성 후에는 C의 당시 길드가 Y여야 한다.
6. 학생 브랜드명/길드명/길드 속성을 이후 변경해도 S1 snapshot 값이 바뀌지 않는지 확인.
7. S1 생성 뒤 새 전입생 F를 길드 X에 배정해도 S1 참석 대상에 F가 새로 생기지 않는지 확인.
8. S2에는 F가 포함되는지 확인.

## 6. 학교 출석과 분리
- 학교 출석 상태를 변경해도 길드 세션 참석은 자동 변경되지 않아야 한다.
- 길드 세션에서 참석/불참/인정불참을 독립적으로 저장.
- 세션 CLOSED 후에도 교사가 정정 저장할 수 있어야 한다.

## 7. Realtime
- 학생 길드 화면을 열어 둔 상태에서 교사가 길드 이동 → 학생 화면 현재 길드 갱신.
- 교사가 길드명/속성 수정 → 학생 길드 헤더 갱신.
- 교사가 길드 세션 참석 수정 → 학생 화면 갱신.
- 한 Realtime 채널에 문제가 생겨도 다른 길드 채널이 함께 죽지 않는지 콘솔 확인.

## 8. 권한/회귀
- 학생 계정에서 `teacher_assign_guild_member`, `teacher_create_guild_session` 등 교사 RPC 호출이 거절되는지 확인.
- 홈 / 자산 / 경매 / 출석·과제 / 기록실 기존 메뉴 정상.
- 교사 모바일 화면에서 하단 메뉴가 겹치지 않고 가로 스크롤 가능.
