# B.R.A.N.D 2.0 — Auction Operations v2 (2026-08-11)

## 적용
기존 Feature 4.1.4 DB까지 적용된 환경에서 Supabase SQL Editor로 다음 파일을 **1회 실행**합니다.

`supabase/APPLY_AUCTION_OPERATIONS_V2.sql`

## 추가 기능
- 즉시 종료·정산 후 교사 화면에 상품/낙찰자/낙찰금액 결과 확인창 표시 (5초 자동 닫힘)
- 같은 학년도·회차의 완료 경매 재생성 지원
  - 첫 생성 요청에서는 기존 기록 존재 여부만 반환
  - 교사 확인 후에만 초기화
  - 기존 `AUCTION_PAYMENT` 거래는 `reverse_transaction()`으로 먼저 환급
  - 거래 감사 기록은 보존하고 경매 상세 기록만 삭제
- 경매 운영 화면에 이전 완료 회차 기록 조회 추가
  - 회차별 상품 수/낙찰 수/유찰 수/총 낙찰액
  - 상세 화면은 학생별 구매 상품 묶음으로 표시
- 상품 프리셋
  - 카테고리: 자리 / 1인1역 / 급식순서 / 특별경매 / 기타
  - 자리 1~24번, 급식순서 1~24번 즉시 일괄 추가
  - 1인1역·특별경매·기타는 학급 DB에 프리셋 영구 저장 후 다음 회차 재사용
  - 현재 회차에 같은 카테고리+상품명이 이미 있으면 일괄 추가 시 자동 건너뜀

## 검증 제한
현재 실행 환경은 npm registry의 일부 패키지가 캐시에 없어 전체 `npm ci`를 완료할 수 없었습니다.
수정한 TS/TSX 파일은 TypeScript `transpileModule` parser 기준 문법 오류 0개를 확인했습니다.
실제 PC에서는 `npm ci` 후 `npm run build`를 먼저 실행하세요.
