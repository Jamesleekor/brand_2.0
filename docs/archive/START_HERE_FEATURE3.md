# B.R.A.N.D 2.0 — 실시간 온라인 경매 적용 안내

## 1. 적용 순서

1. Supabase Dashboard → SQL Editor → New query
2. 다음 파일 전체 실행

```text
supabase/migrations/20260807_01_live_online_auction.sql
```

3. 결과표에서 새 컬럼, 함수 권한, Realtime publication을 확인
4. 프로젝트 루트에서 실행

```bash
npm ci
npm run build
npm run dev
```

## 2. 화면 위치

학생:

```text
시장 → 경매
```

교사:

```text
교사 운영 → 경매 진행
```

교실 앞 중계 화면:

```text
교사 경매 진행 → 중계 화면
```

## 3. 핵심 규칙

- 기본 상품 타이머는 생성 시 설정하며 초기값은 30초입니다.
- 새 최고 입찰이 들어왔을 때 기존 남은 시간이 15초보다 짧으면 마지막 입찰 시점부터 15초가 남도록 서버가 연장합니다.
- 15초를 더하는 방식이 아니라 최소 15초를 다시 보장하는 방식입니다.
- 즉시 입찰은 서버가 `CEIL(현재가 × 1.1)`로 계산합니다.
- 최고 입찰액만큼 GOLD가 예약되어 다른 거래에 사용할 수 없습니다.
- 낙찰 정산은 DB 잠금 안에서 한 번만 처리됩니다.
- 입찰이 없으면 기존 1~3차 유찰 규칙을 유지하며 학급 설정의 할인율을 적용합니다.

## 4. 중요한 운영 조건

초 단위 pg_cron을 사용하지 않으므로, 타이머 만료 정산은 열려 있는 학생·교사·중계 화면 중 하나가 멱등 RPC를 호출하는 방식입니다. 실제 경매 중에는 교사 중계 화면을 계속 열어두는 것을 권장합니다. 모든 화면이 닫힌 경우에도 다음 화면이 열릴 때 서버 만료 여부를 확인하여 정산됩니다.

## 5. 먼저 할 테스트

- 테스트 상품 2~3개
- 시작가 10 GOLD
- 테스트 학생 두 명
- 기본 타이머 30초, 연장 15초

상세 절차는 `FEATURE3_LIVE_AUCTION_TEST_PLAN.md`를 따르세요.
